# firma-ec — Deploy Log

Curated record of production deploys to `app.firmar.ec` (PWA) and `firmar.ec` (landing). Registry: `<REGISTRY>` (IDK Swarm). For narrative detail see [`CHANGELOG.md`](./CHANGELOG.md).

## Hotfixes (post-F7)

| Tag              | Date       | Image                                       | Highlights                                                                                                                                                                  | Commit    |
| ---------------- | ---------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| `pwa 0.26.1` (CI) | 2026-09-09 | `firma-ec-pwa:sha-656f2ffba7a8` (Swarm 2/2 IAS01+IAS02 via CI de Gitea; verificado live: 404 de `/assets/` -> `no-store`, entry -> 200 `immutable`, bundle 0.26.1 con «Razón social») | **El CI vuelve a desplegar: el deploy no se disparaba por un CR suelto en `deploy.yml`, no por el runner.** YAML trata un CR como salto de linea: partia la linea en dos, el resto quedaba en columna 1 fuera del bloque `run: |` y el documento dejaba de parsear. **Gitea ignora un workflow invalido en silencio** (`DetectWorkflows() [W] ignore invalid workflow`), asi que el push pasaba y simplemente no se creaba ningun run — el sintoma apunta al runner y por ahi se fue el diagnostico: se reinicio, se purgaron sus 59 directorios de trabajo (73 GB, disco de IAS01 78% -> 53%) y la 0.26.1 salio a mano. Todo eso hacia falta, pero la causa era el YAML. Ahora `tr -d '
'` en forma octal, que sobrevive a cualquier herramienta que reescriba el fichero. Guardas nuevas: `apps/landing/test/workflows-parseables.test.ts` (ningun workflow puede llevar CR ni tabuladores; probada en rojo) y el timer `act-runner-workdir-clean` en IAS01 (diario 04:30, borra los directorios de +2 dias de `/opt/act-runner-*/work`; probado en las dos direcciones: uno de hace 10 dias se borra, uno recien creado sobrevive). Nota: `act_runner` SI limpia su workdir cuando el job termina bien — los 73 GB eran restos de jobs fallidos. Rollback: `firma-ec-pwa:0.26.1`. | `656f2ff` |
| `pwa 0.26.1` | 2026-09-09 | `firma-ec-pwa:0.26.1` (Swarm `firma-ec_pwa` 2/2 converged IAS01+IAS03; **desplegado a mano** con `scripts/deploy-pwa.sh` porque el runner de Gitea se colgo — ver nota; verificado live: 404 de `/assets/…` -> `no-store`, asset real -> `immutable`, bundle sirve 0.26.1 + «Razón social») | **Un 404 bajo `/assets/` se cacheaba un AÑO y dejo la app en blanco 7 minutos.** La politica de cache se declara por RUTA (`@hashed path_regexp`) y Caddy la evalua ANTES de saber si el fichero existe, asi que el 404 salia con el mismo `max-age=31536000, immutable` que un chunk real; `handle_errors` no tocaba la cabecera. En un rolling update `start-first` conviven replica vieja y nueva unos segundos: la peticion al chunk NUEVO cayo en la VIEJA -> 404 -> cacheado un año en Cloudflare (`cf-cache-status: HIT`, age 213) y en el navegador, que con `immutable` **ni revalida**. Los usuarios recurrentes no lo veian (su Service Worker servia la version anterior completa), lo que lo disfrazo de «no se desplego». Fix a la raiz: `handle_errors { header Cache-Control "no-store" }`, verificado en las DOS direcciones sobre la imagen real antes de desplegar. El smoke post-deploy lo afirma ahora sobre la respuesta real de produccion. El bump de version no es cosmetico: cambia el hash del entry (`index-CFJF4amA.js`), asi que los navegadores que guardaron el 404 piden un nombre nuevo y se recuperan solos. ⚠️ **El CI de Gitea NO ejecuto este deploy**: el runner llevaba 41 dias en marcha con 59 directorios de trabajo (73 GB) y se colgo en su bucle de limpieza (`errSymlink` por los symlinks de pnpm) sin tomar la tarea; reiniciarlo no basto (el run anterior quedo sin cerrar y `concurrency: deploy-prod` retenia al siguiente). Pendiente: limpiar `/opt/act-runner-firmarec/work` y programar limpieza periodica. Rollback: `firma-ec-pwa:sha-b6043e86ac4e`. | `434c78a` |
| `pwa 0.26.0` | 2026-09-09 | `firma-ec-pwa:sha-b6043e86ac4e` (Swarm 2/2 converged via CI de Gitea; verificado live con DOS .p12 reales de representante legal) | **«Validar certificado» distingue al representante legal: RUC, Razón social y Cargo.** Un certificado de representante legal identifica a DOS partes —la persona que tiene la llave y la empresa por la que firma— y la ficha solo mostraba a la persona: quien recibia un documento no podia saber si la firma obligaba a una empresa. FirmaEC 5.1.0 si lo muestra. Las ACE publican el par bajo su propio arco OID con los mismos sufijos (verificado contra certificados reales de ArgosData, Security Data y Uanataca): `.5` cargo, `.10` razon social, `.11` el RUC de la EMPRESA. `organization` NO cae al RDN `O` del subject a proposito: ArgosData pone ahi la empresa representada, pero Security Data y el BCE ponen su PROPIO nombre y el fallback atribuiria la identidad del emisor al titular. Cubre las dos colocaciones del arco (extensiones de primer nivel y `otherName` dentro del subjectAltName). E2E nuevo en los dos idiomas con `.p12` generado al vuelo (ningun certificado real entra al repo) + prueba de muerte. Incluye `chore(landing)`: 4 errores de formato tenian `pnpm lint` —y con el, todo el CI— en rojo desde el 8-sep. Live: ArgosData (CORPORACIÓN MDAUTOS SAS / REPRESENTANTE LEGAL) y Uanataca (IDKMANAGER S.A.S. / GERENTE GENERAL), ambos coinciden con FirmaEC 5.1.0. Rollback: `firma-ec-pwa:sha-ab3a6e665823`. | `b6043e8` |
| `pwa 0.22.4` | 2026-08-06 | `firma-ec-pwa:0.22.4` (Swarm `firma-ec_pwa` 2/2 converged; smoke `/`, `/manifest.webmanifest`, `/trust/tsl-ec.json` = 200×3; verificado live: `dist/assets/verify.worker-DfzmAPv5.js` contiene `UANATACA CA1 2021` + fp `9b083dc4…4469a` + la ruta `uanataca-tsa-ca1`; `/api/aia/uanataca-tsa-ca1` responde 200) | **F2 — sello de tiempo (TSA) de UANATACA sin validar, pese a que el certificado del firmante (0.22.3, arriba) ya validaba.** Reportado por el usuario tras probar el mismo documento real en prod. Causa: `packages/tsa-trust` (almacén de confianza para RFC 3161, DISTINTO del que usa el firmador) solo conocía FreeTSA + un placeholder ARCOTEL — cero ACEs ecuatorianas reales. El verificador oficial MINTEL FirmaEC 5.1.0 valida el mismo sello sin advertencias. Fix: (1) bundle estático — nueva CA "UANATACA CA1 2021" (dedicada a timestamping, distinta de las CAs de firma) + su raíz, mezclado automáticamente en `validateTsaCertChain`; (2) mismo fallback AIA de F1, ahora también para sellos de tiempo (`VerifyOptions.fetchTsaAia`, default `true`). Revisión dual (Fable+Opus) sobre la 1ª versión encontró y cerró 3 problemas antes de mergear: el fetch AIA no gateaba por el allowlist del proxy (Opus lo reprodujo con `fetch` interceptado — 10 peticiones reales a `web.uanataca.com` durante `pnpm test`; re-verificado post-fix con guarda dura de red: 0 llamadas), faltaba caché (mismo patrón ya usado en F1), y `matchedRoot` comparaba por CN de texto en vez de identidad DER. `APP_VERSION` (hardcodeado en `version.ts`, ya venía desincronizado desde 0.22.2/0.22.3) corregido a 0.22.4 en el mismo commit. Rollback: `firma-ec-pwa:0.22.3`. | `50a73fd` |
| `pwa 0.22.3` | 2026-08-05 | `firma-ec-pwa:0.22.3` (digest `488be95c323a`; Swarm `firma-ec_pwa` 2/2 converged; smoke `/`, `/manifest.webmanifest`, `/trust/tsl-ec.json` = 200×3; verificado live: `dist/assets/intermediates-CDRh_nPb.js` contiene `UANATACA CA2 2021` + fp `15ceab3…00e69a`) | **UANATACA CA2 2021 (2ª CA subordinada) + F1 fallback AIA caIssuers.** Un PDF real de IDKMANAGER S.A.S. era rechazado "emisor no reconocido": UANATACA emite en paralelo desde una 2ª intermedia no bundleada. Fix: (1) `packages/tsl-ec` bundlea `uanataca-ca2-2021.pem` + 20 intermedias reales más (mismo patrón que el incidente 2016); (2) **F1** — si una intermedia sigue faltando del bundle estático, el firmador intenta resolverla en vivo vía la URL AIA del propio certificado (RFC 5280 §4.2.2.1), con presupuesto de red por documento (`NetworkBudget`) para que un AIA colgado no cuelgue la firma. 2 rondas de revisión independiente (Fable+Opus) encontraron y cerraron 4 HIGH antes de mergear, incluyendo un CRITICAL revertido el mismo día (`chainIncomplete` no debe suavizar `warning` sobre una señal atacante-controlada — ver `CHANGELOG.md`### Security). E2E nuevo: 3 tests Playwright con navegador+Worker+proxy reales (`aia-fallback.spec.ts`). Historia consolidada en un solo commit tras verificación de compilación aislada (worktree limpio). Rollback: `firma-ec-pwa:0.22.1`. | `a026d6c` |
| `landing 0.4.4` | 2026-06-15 | `firma-ec-landing:0.4.4` (Swarm `firma-ec_landing` converged start-first, zero-downtime; smoke 200×3; CSS servido confirma override dark + `bg-[#17823F]`, `bg-[#25D366]`=0) | **A11y — contraste WCAG AA (PageSpeed móvil 97→100)** — `color-contrast` (audit binario) dejaba Accesibilidad en 97. (1) modo oscuro: brand-500 (3.5:1), ink-500 (4.1:1), ink-600 (2.4:1) bajo 4.5:1 en ~74 nodos; (2) botón WhatsApp blanco/#25D366 = 1.98:1 (única falla visible en claro → 97 con miniatura clara). Fix: override **solo dark** `text-brand-500`→`var(--brand-400)` e `text-ink-500`→`var(--ink-400)` (≈7:1, tokens "texto sobre oscuro" ya existentes; sin `@layer` para ganar a `utilities` de UnoCSS; solo texto, bg/border/ring de marca intactos; claro sin tocar) + verde WhatsApp a `#17823F` (blanco 4.88:1, hover `#146E36`). Verificado: Lighthouse móvil build **a11y 100 / color-contrast 0 fallos / perf 99**; prod tras deploy **a11y 100, SEO 100, BP 100**; contraste numérico de cada par ≥4.88:1; dark sellado por cascada (sin-capa + especificidad 0,2,0). anti-hardcoding 🟢. Rollback: `firma-ec-landing:0.4.3`. | `97f7e39` |
| `pwa 0.15.5` | 2026-06-11 | `firma-ec-pwa:0.15.5` (digest `776cd551f2e7`; Swarm `firma-ec_pwa` 2/2 converged; smoke 200×3; footer live 0.15.5; **E2E prod GO**: firmado real del PDF del reporte → 2 firmas, ambas estampas visibles, 0 errores consola) | **Fix multi-firma: el estampado de la firma PREVIA desaparecía (signer 0.8.2)** — reporte real de Leandro (escrito legal con firma FirmaEC de iText): con `/Annots` indirecto, `injectAnnot` anidaba la ref-al-array como elemento de `/Annots` (ilegal ISO 32000-1 §12.5.2) → los visores descartaban el widget previo y el Link de la página. Fix: redefinir el PROPIO objeto array (widget anexado vía pdf-lib xref-chain), página intacta; + catálogo y AcroForm preservados verbatim (antes plantilla 3 claves → perdía `/StructTreeRoot` `/Lang` `/Metadata` `/MarkInfo` `/PageLayout` = SuspiciousModification pyHanko); + `/Fields` indirecto ya no dropea campos previos; + cola incremental latin1 (UTF-8 corrompía bytes copiados, p.ej. `/Lang` UTF-16BE) y literales no-ASCII como hex UTF-16BE. Regression `annots-indirect-multisig.test.ts` (fixture real iText, 2/4 fallan pre-fix). pyHanko sobre output prod: Signature1 `intact+valid` + **FORM_FILLING** (antes OTHER). Rollback: `firma-ec-pwa:0.15.4`. | `8a59042` |
| `pwa 0.15.4` | 2026-06-09 | `firma-ec-pwa:0.15.4` (Swarm `firma-ec_pwa` 2/2 converged; smoke 200; E2E prod multi-firma iText GO) | **Fix multi-firma ObjStm (signer 0.8.1)** — `cannot_add_signature_to_corrupt_pdf` al añadir 2ª firma a PDFs de iText (estructura en object streams comprimidos): pdf-lib actúa como resolver de fallback en `parsePriorPdf` para Catalog/Pages/Page/AcroForm comprimidos; `detectSignatures` ahora extrae `/Contents` del gap del ByteRange (robusto a orden de claves — pyHanko ponía /Contents antes). Regression `objstm-multisig.test.ts` + fixture sintético. Verificado E2E LIVE con el documento iText real que originó el bug → "PDF firmado correctamente", 2 firmas. CF purge_everything. Rollback: `firma-ec-pwa:0.15.3`. | `399a0ca` |
| `pwa 0.15.3` | 2026-06-09 | `firma-ec-pwa:0.15.3` (Swarm `firma-ec_pwa` 2/2 converged; smoke 200; footer live 0.15.3) | **Autoposicionamiento anti-solape del cuadro de firma** — en PDFs con firmas visibles previas (multi-firmante), el cuadro nuevo se autocoloca en un slot libre junto a las firmas existentes (página de la última firma) vía pdf.js `getAnnotations()` + helper puro `smartPlacement.ts`; docs sin firmas conservan el default centrado exacto. QA: 9 tests unit, svelte-check 0 err, suite 69/69, E2E Playwright 3 escenarios + auditoría qa-auditor GO. CF purge_everything. Rollback: `firma-ec-pwa:0.15.2`. | `eed61d0` |
| `pwa 0.11.0` | 2026-06-02 | `firma-ec-pwa:0.11.0` (Swarm `firma-ec_pwa` 2/2 converged; smoke 200) | **Rebrand PWA con isotipo ƒ** (app.firmar.ec) — `favicon.svg` + `icon-192/512.png` a ƒ sobre tile claro; `Header.svelte` con isotipo ƒ (rúbrica `currentColor` dark-safe) + ".ec" ámbar junto al chip "app". `theme_color`/tokens sin cambios. Verificado live: header ƒ, favicon.svg=ƒ, icon-512 17405B. CF purge_everything. SW: usuarios verán el ícono tras aceptar update. Rollback: `firma-ec-pwa:0.10.0`. | `b592dd4` |
| `landing 0.3.2` | 2026-06-02 | `firma-ec-landing:0.3.2` (Swarm `firma-ec_landing` 2/2 converged; smoke 200) | **ƒ consolidado (decisión final).** Reconcilia el repo tras la excursión revert 0.3.1: el landing en prod nunca dejó de ser ƒ (0.3.0); 0.3.2 deja repo=prod con el ƒ. Sin cambios visuales vs 0.3.0. Parte del rollout "ƒ en toda la app" junto a pwa 0.11.0. | `b592dd4` |
| `landing 0.3.1` | 2026-06-02 | (NO desplegado) | **Revert del ƒ — descartado.** Se commiteó (`8b9924b`) la reversión a la identidad F+check verde pero **nunca se desplegó**; prod siguió en ƒ 0.3.0. Revertido por decisión de mantener el ƒ. Histórico. | `8b9924b` |
| `landing 0.3.0` | 2026-06-02 | `firma-ec-landing:0.3.0` (Swarm `firma-ec_landing` 2/2 converged; smoke 200) | **Re-brand identidad visual** — nuevo isotipo **ƒ caligráfica** (firma manuscrita) navy `#1E3A8A` + travesaño ámbar `#C9821E`, reemplaza el F+check verde genérico (colisionaba con ArQlik/Frecuento, verificado reverse-image Lens). Iconos (favicon svg/ico, apple-touch, icon-192/512) → ƒ sobre tile claro; Header con isotipo ƒ + ".ec" ámbar (dark-mode safe); OG navy+ƒ (Geist conservado). Scope iconos+header+OG (tokens/fuente del sitio intactos). CF `purge_everything` zona firmar.ec → edge MISS confirmado (favicon.svg=ƒ, og 105681B). **Regla: app-icon tile CLARO, nunca ƒ blanca en cuadrado azul.** Respaldo: branch `backup/pre-rebrand-firmar-20260602-1757`. Rollback: `firma-ec-landing:0.2.1`. | `c4d70df` |
| `landing 0.1.26` | 2026-05-23 | `firma-ec-landing:0.1.26` (Swarm 2/2 converged; smoke 200) | **P1 SEO — autoría E-E-A-T (YMYL)**: `TechArticle` de las guías con `author` + `reviewedBy` = **Equipo IDK Manager** (Organization, idkmanager.com) + byline visible "Por Equipo IDK Manager · {fecha}" (ES) / "By IDK Manager Team" (EN). Verificado live. Rollback: `firma-ec-landing:0.1.25`. | `e5fd67e` |
| `landing 0.1.25` | 2026-05-23 | `firma-ec-landing:0.1.25` (Swarm 2/2 converged; smoke 200) | **P0 SEO — cluster de guías enlazado**: columna "Guías" en el footer (todas las páginas, incl. home) enlaza las 5 páginas de contenido antes huérfanas de enlaces internos (firma-electronica-ecuador, bce, pades, comparativos firmaec/adobe-sign), ES+EN. Verificado live: home enlaza las 5, todas 200. Reparte PageRank interno al contenido informacional. Rollback: `firma-ec-landing:0.1.24`. | `ff59f67` |
| `landing 0.1.24` | 2026-05-23 | `firma-ec-landing:0.1.24` (Swarm `firma-ec_landing` 2/2 converged; smoke 200) | **P3 SEO post-auditoría**: `public/favicon.ico` multi-res 16/32/48 (antes 404 → ahora 200 `image/vnd.microsoft.icon`); redirect 301 `/en/firma-electronica-ecuador/` → `/en/electronic-signature-ecuador/` (Caddyfile, evita soft-404). Verificado live + sin regresión (CSP cloudflareinsights OK, home 200, sitemap huérfano 404). Nota: SSH se cortó en el 1er intento durante `docker build` (sin deploy parcial, servicio quedó en 0.1.23); reintento limpió. Rollback: `firma-ec-landing:0.1.23`. | `88648a8` |
| `landing 0.1.23` | 2026-05-23 | `firma-ec-landing:0.1.23` (Swarm `firma-ec_landing` 2/2 converged; smoke 200) | **Auditoría SEO técnica — quick wins** (skill `search-growth`). LIVE verificado: canonical/sitemap/hreflang/schema/llms ahora con trailing slash auto-referencial (`/firma-electronica-ecuador/`=200, ya no 308); ECIs unificadas a "16 de las 17" (glosario/acerca/guía eran "8" stale); `public/sitemap.xml` huérfano → **404** (requirió `rm -rf /root/firma-ec-build` en IAS: `tar xzf` no borraba el archivo eliminado — patrón deploy-dir-stale); meta desc home ≤155. **CSP RESUELTA**: el header CSP NO lo pone el Caddyfile sino el middleware `firma-headers` de **Traefik** (file provider `/mnt/truenas/traefik_certs/cloudflare/dynamic.yml`), que sobrescribe el del origen. Se añadió `static.cloudflareinsights.com` (script-src) + `cloudflareinsights.com` (connect-src) al live dynamic.yml (backup `/root/_backups/traefik-dynamic-20260523-210746.yml`). ⚠️ El `--providers.file.watch=true` NO propaga sobre el mount NFS de TrueNAS → editar el archivo no basta: hubo que `docker service update --force traefik_traefik` (servicio **global**, 1 task/nodo) para que las **3 réplicas** relean el config (una corrida cicló IAS02, otra IAS01+IAS03). Verificado tras el restart: edge CSP 8/8 con cloudflareinsights, **0 errores CSP en consola** (Chrome), beacon de CF Web Analytics carga. Repo `infra/traefik/{middlewares,dynamic.merged}.yml` reconciliados con el live (CSP, COEP credentialless, noindex solo en firma-pwa). Rollback: `firma-ec-landing:0.1.21` + restaurar backup dynamic.yml. | `a336c81` |
| `landing 0.1.21` | 2026-05-23 | `firma-ec-landing:0.1.21` (Swarm 2/2 converged; smoke 200; home ES/EN sirven el strip) | **Strip de patrocinadores en la home** (`SponsorsStrip.astro`, antes de `OperadoPor`): muro de logos data-driven + empty-state con CTA a `/patrocinar` mientras no haya sponsors. LIVE en `firmar.ec/` y `firmar.ec/en/`. Rollback: `firma-ec-landing:0.1.20`. | `f808eaf` |
| `landing 0.1.20` | 2026-05-23 | `firma-ec-landing:0.1.20` (Swarm `firma-ec_landing` 2/2 converged; smoke 200; live `firmar.ec/patrocinar`=200) | **Programa de patrocinio**: nueva página `/patrocinar` (`/en/sponsor`) — tiers Bronze/Silver/Gold/Platinum/Founding, **pago directo por transferencia + factura SRI, sin intermediarios**. Enlace en nav y footer, bilingüe, sobre tokens existentes (OKLCH/Geist/lucide), verificado claro/oscuro/móvil. Fix de build: `landing.Dockerfile` Node 20 → **22-alpine** (lockfile pide `undici@8.3.0` que requiere Node ≥22.19; alineado con CI y `pwa.Dockerfile`). Rollback: `firma-ec-landing:0.1.19`. | `73ba6dc` |
| `v0.8.1`         | 2026-05-22 | `firma-ec-pwa:0.8.1` (Swarm `firma-ec_pwa` converged; smoke 200; bundle live reporta 0.8.1) | **Fix crash `Result.svelte`**: `result.integrity` queda `undefined` en el path `catch` de `verifyPdf` (firma con error de motor → `status:'invalid'` sin integrity); el acceso sin guardia tiraba `TypeError` y rompía la tarjeta de resultado. Guarda con `result.integrity && …`; svelte-check 0 errores. | `c3a5767` |
| `landing 0.1.19` | 2026-05-22 | `firma-ec-landing:0.1.19` (Swarm `firma-ec_landing` converged; smoke 200) | **Sección Compatibilidad actualizada** tras v0.8.0: de "8 ECIs activas" → **"Reconocemos las 17 ECIs acreditadas por ARCOTEL"** (16 con raíz real propia + Registro Civil cubierta vía BCE/Security Data). Rediseño UI Pro Max: strip de stats (16 raíces propias · 28 raíces · 100% cobertura), grilla de 16 ECIs mostrando el CN de la raíz embebida por cada una, nota Registro Civil. LIVE en `firmar.ec`. | `4055c17` |
| `v0.8.0`         | 2026-05-22 | `firma-ec-pwa:0.8.0` (Swarm `firma-ec_pwa` converged; smoke app/manifest/tsl-ec.json = 200) | **Raíces ACE reales (placeholders → 28/29)** + **nueva pantalla Validar Certificado** (`/validar-certificado`). Reporte de firmante real (Leandro Gorina, cert **LAZZATE** "Persona Natural EXT"): la verificación fallaba con su cert porque LAZZATE seguía como placeholder en prod, y su cert encadena a **Lazzate Root CA2** (vintage no desplegado). Fix: 8 ACE placeholder → raíz real de la librería oficial MINTEL FirmaEC (lazzate, alpha-technologies, appfirmas, corpnewbest, darkcam, firmasegura, letmi, primecorelat) + anclas paralelas multi-vintage (lazzate-ca1/ca2/wego, anfac-2016/2024, argosdata-2026, datil-2025). TSL 1.10.0 → **1.11.0** (seq 12). LIVE: `tsl-ec.json` sirve `lazzate-ca2` fp `aa9d6d0d…`; cert real de Leandro → `trusted=true`. `checkCertificate()` nuevo en verifier 0.7.9. | `c007cd4` |
| `v0.7.12`        | 2026-05-15 | `firma-ec-pwa:0.7.12` + `firma-ec-landing:0.7.12` (re-tagged) | **Registro Civil marcado `isDefunct: true`**. Evidencia: resolución 009-DIGERCIC-2025 firmada por 4 funcionarios — Director Rivadeneira + analista Garnica firman con certs BCE; Oquendo + Rentería firman con certs Security Data CA-2. RC no opera raíz PKI propia. **8/8 ACEs activas reales** = demo mode OFF para PDFs firmados con cualquiera de las 8 raíces cargadas. | `bc128a0` |
| `v0.7.11`        | 2026-05-15 | `firma-ec-pwa:0.7.11` + `firma-ec-landing:0.7.11` (re-tagged) | **Judicatura iCert-EC Real Root CA** extracted from PAdES CMS chain of multi-signer judicial PDF (Kevin's `075-2026.pdf`, 3 firmas iCert-EC). Self-signed `CN=ICERT-EC ENTIDAD DE CERTIFICACION RAIZ`, valid 2014-10-16 → 2034-10-16. **8/9 ACEs activas reales** (was 7/9). Solo Registro Civil queda placeholder. | `413d138` |
| `v0.7.10`        | 2026-05-15 | `firma-ec-pwa:0.7.10` + `firma-ec-landing:0.7.10` (re-tagged) | **Security Data legacy Root CA** parallel anchor (`fc8d6968…`, valid 2011-02-16 → 2031-02-16) loaded as new slug `securitydata-legacy`. Found in 6 PAdES CMS chains across production PDFs. New `isParallelAnchor` flag keeps banner count at 7/9 (legacy root is same org as CA-2 slug). TSL bumped 1.7.0 → 1.8.0 (seq 9). 18 roots total / 8 real. | ``9f94a1e`` |
| `v0.7.9`         | 2026-05-15 | `firma-ec-pwa:0.7.9` (digest `844972337995`) + `firma-ec-landing:0.7.9` (digest `4cd2a6a567d4`, re-tagged) | **ANFAC Ecuador Real Root CA** discovered by scanning all 114 PAdES-signed PDFs in `~/Nextcloud/Documentos`. Self-signed cert `CN=ANF High Assurance Ecuador Root CA`, valid 2019-10-17 → 2039-10-12. 7/17 ACEs real. Banner reports **7 de 9 ACEs activas** (was 6/9). 2 placeholders left: Judicatura, Registro Civil. | `bf1f163` |
| `v0.7.8`         | 2026-05-15 | `firma-ec-pwa:0.7.8` (digest `966ef488b163`) + `firma-ec-landing:0.7.8` (digest `4cd2a6a567d4`, re-tagged from 0.7.7) | **Header redirect to landing**: lockup (`firmar.ec [app]`) and the "Inicio / Home" nav item now point to `https://firmar.ec/` (institutional landing) instead of the internal SPA `/` route. PWA `Home.svelte` still exists for installed-app entry. Verified LIVE on Chrome: `logo_href: "https://firmar.ec/"`, `inicio_href: "https://firmar.ec/"`. | `f9766fd` |
| `v0.7.7`         | 2026-05-15 | `firma-ec-pwa:0.7.7` (digest `d64a38b0d014`) + `firma-ec-landing:0.7.7` (digest `4cd2a6a567d4`, byte-identical) | **Security Data Real Root CA** extracted from PAdES CMS of `CONTRATO2026 SOLUCIONES…-signed.pdf` (LT-level signature carried the full chain). 6/17 ACEs real. Banner reports **6 de 9 ACEs activas** (was 5/9). Verifier regression test updated so the two SD-signed fixtures now expect `expectedRealRoot: 'securitydata'`. | `7b81911` |
| `v0.7.6`         | 2026-05-15 | `firma-ec-pwa:0.7.6` (digest `143b76b5b6cf`) + `firma-ec-landing:0.7.6` (digest `4cd2a6a567d4`, byte-identical to 0.7.5) | **BCE Real Root CA** extracted from PAdES CMS chain of a Registro Civil-signed Certificado de Matrimonio (Registro Civil signs with BCE-issued certs). 5/17 ACEs real. Banner reports **5 de 9 ACEs activas** (was 4/9). BCE WAF previously blocked the public fetch path — offline extraction was the only route. | `fe20f32` |
| `v0.7.5`         | 2026-05-14 | `firma-ec-pwa:0.7.5` (digest `ba1dff0c2b93`) + `firma-ec-landing:0.7.5` (digest `4cd2a6a567d4`) | Datil real CA loaded (4/17 ACEs real). New `isDefunct` flag → 8 inactive ACEs hidden from active denominator. Banner now reports **4 de 9 ACEs activas** (was 3/17). Landing `OperadoPor` shows official IDK Manager wordmark image instead of plain text. | `e8be5fc` |
| `pwa 0.7.0-rc9`  | 2026-05-10 | `firma-ec-pwa:0.7.0-rc9` (digest `448aa89a`) | **fix(Button):** internal `href="/x"` rewritten to `"#/x"` so Home Hero CTAs (`Verificar PDF` / `Firmar PDF`) navigate inside the installed PWA. APP_VERSION → `0.7.0-rc2`. | `96d4b90` |

## F6 — PAdES B-T (RFC 3161 timestamping)

| Tag             | Date       | PWA image digest                                                          | Landing image digest                                                      | Highlights                                                                                                              | Commit    |
| --------------- | ---------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | --------- |
| `v0.6.0-rc7`    | 2026-05-10 | `sha256:d71885e39a6a3a6856057c90d578af42f83ba1d454ab10d1c429971a3e915036` | `sha256:70d15a76020931a93a046879f2bd1baf05ebd0552b80a02c4ec1b448082fa4bb` | F6.7 TSL real PEM fetch (2/17 ACEs: Eclipsoft + Uanataca). Granular `TRUST_PARTIAL` banner. Landing hero `.p12` copy.   | `08f65fd` |
| `v0.6.0-rc6`    | 2026-05-10 | `sha256:08c56592dcef668d29e7998d99dc2a3f2c5a5eb9d58c3583c74c6265bb3b3c8e` | (unchanged v0.1.8)                                                        | F6.6 TimestampBadge gold variant `success-green` — cosmetic only.                                                       | `af69a93` |
| `v0.6.0-rc5`    | 2026-05-10 | `sha256:0e1927e90f7fef9ea266bd1ad715014c040100a7d540edba897db4917c26d6ae` | (unchanged)                                                               | F6.5 verifier extracts B-T timestamp + bumps engine version. New `@firma-ec/tsa-client` + `@firma-ec/tsa-trust`.        | `f9b2b02` |
| `v0.6.0-rc4`    | 2026-05-10 | `sha256:779846679998f5cb67b913fda4233d17e93659d351a2e90509aa3cfc757c3fc5` | (unchanged)                                                               | F6.4 signer 0.5.0-rc3 — TSA timestamp embedding in PAdES.                                                               | `37dad8a` |
| `v0.6.0-rc3`    | 2026-05-10 | `sha256:2bff022e94fc522bc8a6b8e28c4b08b335a459ff6db274b907a903da04b7da2c` | `sha256:6ccd3483d6bbef955fbed5a01549bb169fa2c63937f5185d1b06dff4c804e968` | F6.3 redirect after sign + landing v0.1.8 partner.                                                                      | `640cdaa` |
| `v0.6.0-rc2`    | 2026-05-10 | `sha256:93af81b60e2bf331502e72946b8d2a2c1ef3cbdd69ceb2c558acd96ab6dfbbbc` | (unchanged)                                                               | F6.2 — Configuración TSA pane + post-deploy QR deep-link verified.                                                      | `5137c5e` |
| (rc1 untagged)  | 2026-05-10 | `sha256:aea860ce4299b23a7714531c74ed3bb1188c371647551f041ea93b6e912ba621` | (unchanged)                                                               | F6 T25 infra — `/api/tsa` proxy route block + SPA fallback. (no formal tag — superseded by rc2.)                        | `2205f7b` |

## F5 — Visual parity & light default

| Tag       | Date       | Highlights                                                              | Commit    |
| --------- | ---------- | ----------------------------------------------------------------------- | --------- |
| `v0.5.1`  | 2026-05-09 | Landing v0.1.7 light default + CHANGELOG.                               | `1b4c065` |
| `v0.5.0`  | 2026-05-09 | Deep visual parity landing↔PWA (tokens + Hero/Footer/Button patterns). | `fc41784` |
| `v0.4.9`  | 2026-05-09 | Landing v0.1.6 — UnoCSS Wind4 container fix (flush-left bug).           | `47b04fd` |
| `v0.4.8`  | 2026-05-09 | Landing↔App linkage + UI Pro Max P0 polish.                             | `f8e759b` |
| `v0.4.7`  | 2026-05-09 | ECDSA P-256/P-384/P-521 PFX signing.                                    | `fb967a2` |
| `v0.4.6`  | 2026-05-09 | Polish bundle.                                                          | `d9d5f22` |
| `v0.4.5`  | 2026-05-09 | F3+F4 LIVE — visible signature with QR (FirmaEC-style 240×72pt).        | `f97934e` |

## Live verification (rc7)

- Landing ES h1: `Firma y verifica PDFs con tu certificado electrónico .p12.` ✓
- Landing EN h1: `Sign and verify PDFs with your .p12 electronic certificate.` ✓
- App `/configuracion` footer: `versión 0.6.0-rc7` ✓
- App `/verificar` con `sample-b-t-freetsa.pdf`: TSA stamp `Sellada por TSA · Emitido por www.freetsa.org`, PAdES `B-T`, banner `TRUST_PARTIAL` 2/17 ✓
- CSP includes `https://freetsa.org` in `connect-src` ✓

## Caveats

- **SW cache**: existing PWA users on rcN-1 must accept the in-app update prompt or hard-reload to pick up rcN (precache hash changes per release).
- **TSL trust**: 15/17 ACEs ARCOTEL still self-signed placeholders; `TRUST_PARTIAL` banner surfaces this. See `CHANGELOG.md` rc7 TODOs.
- **Tags**: `v0.6.0-rc1` was never formally tagged (commit `2205f7b` was superseded by `5137c5e` rc2 within the same hour).
